domain module test {
public reason ParseFailure {
    Bad,
    MissingKey { key: Text },
}

public fn parse() -> Int fails ParseFailure {
    fail Bad
}

public fn main() -> Int {
    match parse() {
        failed(Bad) => 1,
        failed(MissingKey { key }) => 2,
    }
}
}
