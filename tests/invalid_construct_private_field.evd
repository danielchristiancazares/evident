domain module outer {
    public record Resource {
        public x: Int,
        y: Int,
    }
}

domain module client {
    public fn make_resource() -> outer::Resource {
        outer::Resource { x: 1, y: 2 }
    }
}
