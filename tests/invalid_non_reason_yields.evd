foundation module support {
public fn bad_generic<T>() -> Int fails T { 0 }
}

domain module test {
public reason Failure {
    Bad,
}

public state NotAReason {
    Alpha,
    Beta,
}

public fn bad_state() -> Int fails NotAReason { 0 }

public fn bad_builtin() -> Int fails Int { 0 }

public fn bad_collection() -> Int fails List<Failure> { 0 }
}
