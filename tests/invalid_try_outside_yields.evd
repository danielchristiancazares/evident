domain module test {
public reason OpenFailure {
    FileNotFound,
}

public record FileHandle {
    raw: Int,
}

public fn open() -> FileHandle fails OpenFailure

public fn boot() -> FileHandle {
    try open()
}
}
