domain module test {
public reason OpenFailure {
    FileNotFound,
}

public record FileHandle {
    raw: Int,
}

public fn acquire() -> FileHandle fails OpenFailure { fail FileNotFound }

public fn boot() -> FileHandle {
    try acquire()
}
}
