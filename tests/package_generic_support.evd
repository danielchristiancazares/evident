foundation module package_generics {
public record Box<T> {
    public value: T,
}

public fn identity<T>(value: T) -> T {
    value
}

public fn wrap<T>(value: T) -> Box<T> {
    Box<T> { value: identity<T>(value) }
}

public fn unwrap<T>(box: Box<T>) -> T {
    box.value
}
}
