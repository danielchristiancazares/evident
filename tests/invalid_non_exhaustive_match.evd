domain module test {
public state FeatureMode {
    OfflineOnly,
    ProviderBacked { config: Text },
}

public fn render(mode: FeatureMode) -> Text {
    match mode {
        OfflineOnly => "offline",
    }
}
}
