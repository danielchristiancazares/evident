domain module test {
public reason ParseFailure {
    Bad,
}

public fn parse() -> Int fails ParseFailure {
    fail Bad
}

public fn main() -> Int {
    match parse() {
        succeeded(value) => value,
        Bad => 1,
        failed(Bad) => 2,
    }
}
}
