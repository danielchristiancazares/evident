boundary module test {
public proof Receipt {
    count: Int,
}

public fn bad(entries: NonEmptyList<MapEntry<Text, Receipt>>) -> NonEmptyMap<Text, Receipt> {
    nonempty_map_from_entries_using_last_bindings<Text, Receipt>(entries)
}

public fn main() -> Int {
    0
}
}
