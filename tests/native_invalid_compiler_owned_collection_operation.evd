domain module test {
public fn main() -> Int {
    let left = map_empty<Text, Int>()
    let right = map_empty<Text, Int>()
    match map_merge_rejecting_shared_keys<Text, Int>(left, right) {
        succeeded(entries) => 0,
        failed(InputsHadSharedKey) => 1,
    }
}
}
