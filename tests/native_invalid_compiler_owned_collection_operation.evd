domain module test {
public fn main() -> Int {
    let empty = map_empty<Text, Int>()
    match map_remove_bound<Text, Int>(empty, "one") {
        succeeded(entries) => 0,
        failed(RequestedKeyHadNoBinding) => 1,
        failed(RequestedKeyAlreadyHadBinding) => 2,
    }
}
}
