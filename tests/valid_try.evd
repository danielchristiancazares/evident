domain module test {
public reason OpenFailure {
    FileNotFound { path: Text },
}

public record FileHandle {
    raw: Int,
}

public fn acquire(raw: Int) -> FileHandle fails OpenFailure {
    FileHandle { raw }
}

public fn boot(raw: Int) -> FileHandle fails OpenFailure {
    try acquire(raw)
}
}
