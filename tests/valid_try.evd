public reason OpenFailure {
    FileNotFound { path: Text },
}

public struct FileHandle {
    raw: Int,
}

public fn open(raw: Int) -> FileHandle yields OpenFailure {
    FileHandle { raw }
}

public fn boot(raw: Int) -> FileHandle yields OpenFailure {
    try open(raw)
}
