domain module test {
public reason OpenFailure {
    FileNotFound { path: Text },
}

public record FileHandle {
    raw: Int,
}

public fn boot() -> FileHandle fails OpenFailure {
    fail FileNotFound { path: "missing" }
}
}
