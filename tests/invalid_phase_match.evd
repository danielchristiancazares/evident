domain module test {
public phase AppConfig {
    fields {
        public id: Int,
    }

    positions {
        Draft,
        Validated,
    }
}

public fn bad(config: AppConfig::Draft) -> Int {
    match config {
        AppConfig::Draft => 1,
    }
}
}
